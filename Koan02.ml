(* string bois *)


(* string stuff doesnt belong here *)

(* words can be be put together as well. *)
let stringadd =
  "this " ^ "is " ^ "true" = _

(* but once put together, a word isn't simple to adjust. *)
(* String.sub string start length *)
let str = 
  "true this is as well"

let stringsub =
  "true" = _